<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="3dp"
    app:cardBackgroundColor="#0a0909"
    app:cardCornerRadius="2dp"
    app:cardElevation="0dp"
    app:strokeColor="#272727"
    app:strokeWidth="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingTop="20dp"
        android:paddingBottom="16dp"
        android:paddingHorizontal="12dp"
        android:gravity="center_horizontal">

        <!-- Status Label (matches frontend: fontSize 16, letterSpacing 2) -->
        <TextView
            android:id="@+id/statusText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="VACANT"
            android:textColor="#444444"
            android:textSize="16sp"
            android:fontFamily="monospace"
            android:letterSpacing="0.125" />

        <!-- Circular Progress (matches frontend: size 80) -->
        <com.parksense.android.ui.views.CircularProgressView
            android:id="@+id/circularProgress"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:layout_gravity="center"
            android:layout_marginTop="16dp" />

        <!-- Timer (matches frontend: fontSize 13, marginTop 12) -->
        <TextView
            android:id="@+id/timerText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="00h:00m:00s"
            android:textColor="#ededed"
            android:textSize="13sp"
            android:fontFamily="monospace"
            android:layout_marginTop="12dp" />

        <!-- Slot Name (matches frontend: fontSize 20, marginTop 8) -->
        <TextView
            android:id="@+id/slotNameText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="P1"
            android:textColor="#ededed"
            android:textSize="20sp"
            android:fontFamily="monospace"
            android:layout_marginTop="8dp" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
