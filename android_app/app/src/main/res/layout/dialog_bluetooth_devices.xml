<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@color/dialog_background"
    android:minWidth="320dp">

    <!-- Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="12dp"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:background="#000000">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/bluetooth_devices_title"
            android:textSize="20sp"
            android:textColor="@color/text_primary"
            android:fontFamily="monospace"
            android:letterSpacing="0.05" />

        <TextView
            android:id="@+id/btnClose"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="✕"
            android:textSize="20sp"
            android:textColor="#444444"
            android:fontFamily="monospace"
            android:padding="8dp"
            android:clickable="true"
            android:focusable="true" />
    </LinearLayout>

    <!-- Divider -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/border" />

    <!-- Error Container (hidden by default) -->
    <TextView
        android:id="@+id/txtError"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#33BA2D2D"
        android:padding="8dp"
        android:layout_marginHorizontal="12dp"
        android:layout_marginTop="8dp"
        android:textSize="12sp"
        android:textColor="#BA2D2D"
        android:fontFamily="monospace"
        android:gravity="center"
        android:visibility="gone" />

    <!-- Device List -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerDevices"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="150dp"
        android:maxHeight="320dp"
        android:padding="12dp"
        android:scrollbars="vertical"
        android:clipToPadding="false" />

    <!-- No Devices Message -->
    <LinearLayout
        android:id="@+id/layoutNoDevices"
        android:layout_width="match_parent"
        android:layout_height="150dp"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="20dp"
        android:visibility="gone">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/bluetooth_no_devices"
            android:textSize="14sp"
            android:textColor="#444444"
            android:fontFamily="monospace"
            android:gravity="center" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Pair your HC-05 module in phone settings first"
            android:textSize="12sp"
            android:textColor="#444444"
            android:fontFamily="monospace"
            android:gravity="center"
            android:layout_marginTop="8dp"
            android:alpha="0.7" />
    </LinearLayout>

    <!-- Scan Button (at bottom, full width, like frontend) -->
    <Button
        android:id="@+id/btnScan"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:text="@string/bluetooth_scan"
        android:textSize="14sp"
        android:textColor="@color/text_primary"
        android:background="@drawable/button_scan"
        app:backgroundTint="#444444"
        android:fontFamily="monospace"
        android:textAllCaps="true"
        android:letterSpacing="0.05" />

    <!-- Progress indicator for scanning -->
    <FrameLayout
        android:id="@+id/layoutScanning"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:background="@drawable/button_scan"
        android:visibility="gone">

        <ProgressBar
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_gravity="center"
            android:indeterminateTint="@color/text_primary" />
    </FrameLayout>

</LinearLayout>
